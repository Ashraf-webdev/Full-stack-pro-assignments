
// 1. Precedence Example:

let resultA = 5 + 6 * 4; // Multiplication has higher precedence, so it's evaluated first.

// 2. Example of Associativity:

let resultB = 10 - 5 - 2; // Subtraction is left-associative, so it's evaluated from left to right.

